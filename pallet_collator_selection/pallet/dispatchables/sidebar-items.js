window.SIDEBAR_ITEMS = {"fn":[["leave_intent","Deregister `origin` as a collator candidate. Note that the collator can only leave on session change. The `CandidacyBond` will be unreserved immediately."],["register_as_candidate","Register this account as a collator candidate. The account must (a) already have registered session keys and (b) be able to reserve the `CandidacyBond`."],["set_candidacy_bond","Set the candidacy bond amount."],["set_desired_candidates","Set the ideal number of collators (not including the invulnerables). If lowering this number, then the number of running collators could be higher than this figure. Aside from that edge case, there should be no other way to have more collators than the desired number."],["set_invulnerables","Set the list of invulnerable (fixed) collators."]]};